# An unique identifier for the head node and workers of this cluster.
cluster_name: minimal-docker

# The maximum number of workers nodes to launch in addition to the head
# node.
max_workers: 2

docker:
    enabled: True

# Cloud-provider specific configuration.
provider:
    type: aws
    region: us-west-2
    # Security group to create with custom in bound rules and name.
    security_group:
        GroupName: cloudtik-minimal
    aws_s3a_storage:
        bucket: cloudtik-bucket-bpe
        fs.s3a.access.key: AKIAVCPUOMTADCOOWIQO
        fs.s3a.secret.key: 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

auth:
    ssh_user: ubuntu
    #Set proxy if you are in company network
    ssh_proxy_command: "ncat --proxy-type socks5 --proxy proxy-shz.intel.com:1080 %h %p"

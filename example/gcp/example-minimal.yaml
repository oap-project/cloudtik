# A unique identifier for the head node and workers of this cluster.
cluster_name: minimal

# The maximum number of worker nodes to launch in addition to the head
# node. min_workers default to 0.
max_workers: 2

# Cloud-provider specific configuration.
provider:
    type: gcp
    region: us-central1
    availability_zone: us-central1-a
    project_id: mlp-das-bdf-oap-11228062 # Globally unique project id

# How CloudTik will authenticate with newly launched nodes.
auth:
    ssh_user: ubuntu
    ssh_proxy_command: "nc --proxy-type socks5 --proxy proxy-shz.intel.com:1080 %h %p"

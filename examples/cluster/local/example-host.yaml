# A unique identifier for the cluster.
cluster_name: example

# Cloud-provider specific configuration.
provider:
    type: local

auth:
    # The user is current user with sudo privilege on local host
    ssh_user: ubuntu

# Disable docker for local host mode
docker:
    enabled: False

# A unique identifier for the workspace.
workspace_name: default

provider:
    type: local

# Option whether the workspace is targets to clusters for dockers or host
# We consider local clusters run on docker or host are in different workspace.
# This will affect only the result of command such as list-clusters
docker:
    enabled: True

from: aliyun/gpu/base-4

# Cloud-provider specific configuration.
provider:
    type: aliyun

# 8 * A100-40GB
# The instance configuration for a very large instance type
available_node_types:
    worker-default:
        node_config:
            InstanceType: ecs.gn7e-c16g1.32xlarge

from: aliyun/gpu/base-4

# Cloud-provider specific configuration.
provider:
    type: aliyun

# 2 * T4
# The instance configuration for a medium instance type
available_node_types:
    worker-default:
        node_config:
            InstanceType: ecs.gn6i-c24g1.12xlarge

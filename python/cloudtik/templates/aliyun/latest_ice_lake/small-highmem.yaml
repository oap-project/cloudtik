from: aliyun/latest_ice_lake/small

# Cloud-provider specific configuration.
provider:
    type: aliyun

# The instance configuration for a small instance type
available_node_types:
    worker.default:
        node_config:
            InstanceType: ecs.r7.xlarge

# Cloud-provider specific configuration.
provider:
    type: aws

# The instance configuration for a typical cluster setups
available_node_types:
    head.default:
        node_config:
            InstanceType: m5.4xlarge
    worker.default:
        node_config:
            InstanceType: m5.4xlarge

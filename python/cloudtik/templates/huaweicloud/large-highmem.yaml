from: huaweicloud/large

# Cloud-provider specific configuration.
provider:
    type: huaweicloud

# The instance configuration for a standard server flavor
available_node_types:
    worker.default:
        node_config:
            flavor_ref: m6.16xlarge.8
